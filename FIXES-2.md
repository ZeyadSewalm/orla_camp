# الجولة التانية — إصلاحات الشكل والسرعة والاستقرار

---

## 1. الكلام الداخل في بعضه

**السبب:** كلاس `.display` كان عليه `leading-[0.98]`.

عند 16px الرقم ده عادي. لكن على عنوان بـ 72px (زي `text-[4.5rem]` في الـ final CTA)
صندوق السطر بيبقى أقصر من الخط نفسه، فحروف السطر التاني بتقع فوق خط السطر الأول —
وده بالظبط اللي في الصورة: "keep the control." قاعدة جوه "Learn the workflow."

**الحل:** `leading-[1.06]` — لسه tight وeditorial، بس مفيش تداخل.
والعربي `.display` بقى `line-height: 1.4` (Almarai امتداداته أقصر من Noto Kufi).

## 2. اسم الموقع مش ظاهر جنب اللوجو على الموبايل

ده غلطي من الجولة اللي فاتت — أنا اللي خبّيت الـ Wordmark تحت 400px.

**اترجع ظاهر على كل المقاسات.** اللي بيخرج من الشريط على الموبايل بقى **لينكات
النافيجيشن**، مش اسم البراند.

وكمان الـ wordmark نفسه كان **مقصوص** (باين في صورة الديسكتوب): `leading-[0.76]`
على 3 سطور متراصة كان بيقص أعلى وأسفل الحروف. بقى `0.92`.

## 3. الاتنين معمولهم pin

كان الـ `<header>` كله `sticky` وجواه **صفين**: شريط اللوجو + صف لينكات الموبايل.
فلما تنزل، الاتنين يفضلوا ثابتين فوق بعض وتبقى بتقرا الصفحة من شباك ارتفاعه ~130px.

**الحل:** صف لينكات الموبايل اتنقل **برّه** الـ `<header>` خالص. دلوقتي بيتمرّر مع
الصفحة عادي، وشريط اللوجو بس هو اللي بيفضل ثابت.

تأكدت من الـ HTML المبني: عنصر `sticky` **واحد** بس في الصفحة.

## 4. Tier comparison وحش لما بتسحب بالعرض

ده كمان غلطي من الجولة اللي فاتت — سيبت الجدول بيتمرّر وضفت سطر "اسحب الجدول".

**المشكلة الحقيقية:** عمود **أسماء الصفوف** بيمشي مع السحب. فتفضل باصص على عمود
علامات ✓ من غير ما تعرف بتاعة إيه. وفي العربي أسوأ، لأن السحب بيبدأ من اليمين
وأسماء الباقات بتتقطع نُص كلمة ("Foundati / on").

**الحل:** تحت `md` بقى **كارت لكل باقة** — السعر (مصري ودولي) وتحته قايمة بكل حاجة
داخلة في الباقة، كل ✓ جنب اسمه. **مفيش سحب بالعرض خالص.**
الجدول بيرجع من `md` وفوق، حيث عنده المساحة اللي تخليه أحسن شكل.

## 5. فونت العربي → Almarai

الخطوط بقت self-hosted في `public/fonts/`، و`@font-face` في `globals.css`.

| | قبل | بعد |
|---|---|---|
| الخط العربي | Noto Kufi Arabic | **Almarai** |
| حجم الـ subset العربي | 124 KB | **32 KB** |
| نوع الخط | display (خط عناوين) | نص |

Noto Kufi خط **عناوين** وكان بيتحط على فقرات كاملة — ده سبب كبير في إن العربي كان
شكله تقيل ووحش.

**حاجة مهمة:** Almarai عنده **400 / 700 / 800 بس — مفيش 500 ولا 600**. والتصميم
بيستخدم `font-medium` و`font-semibold` في أماكن كتير، والمتصفح كان هيزوّر الوزن
(fake bold = يمسح الحرف عرضيًا) وشكله بيبقى وحش جدًا. فقفلت الـ synthesis
(`font-synthesis-weight: none`) وحوّلت الأوزان دي لأقرب وزن حقيقي.

كمان شلت الـ subsets اللي عمرها ما هتترسم: `import '@fontsource-variable/inter'`
كان بيجيب **7** تعريفات `@font-face` (cyrillic، cyrillic-ext، greek، greek-ext،
latin، latin-ext، vietnamese) لموقع عربي/إنجليزي. دلوقتي 7 تعريفات إجمالي **لكل
الخطوط مع بعض**.

## 6. الأسعار المكسورة

`ar-EG` بيطلّع أرقام عربية-هندية (٢٢٬٠٠٠). الأرقام دي **مش موجودة في IBM Plex Mono**
اللي كلاس `.figure` بيحطه على كل سعر — فالمتصفح كان بيرجع لخط تاني نُص الرقم، والنتيجة
خليط أحجام وأوزان مختلفة (باين في صورة الأسعار).

بقى `ar-EG-u-nu-latn`:
```
قبل:  ‏١٥٬٠٠٠ ج.م.‏
بعد:  ‏15,000 ج.م.‏
```
نفس الحاجة اتعملت لتواريخ الـ live sessions.

---

## السرعة

- **الخطوط:** بقى بيتحمّل الـ subset المستخدم بس. والصفحة بتعمل `preload` للخط اللي
  هيرسم أول شاشة **حسب اللغة** — عربي ياخد Almarai، إنجليزي ياخد Inter، ومحدش
  بيدفع تمن ملف التاني. من غير الـ preload المتصفح لازم ينزّل الـ CSS → يقراه →
  يكتشف الـ `@font-face` → **وبعدين** يطلب الخط: 3 رحلات ورا بعض قبل ما أي نص حقيقي
  يبان.
- **شلت `three.js`** من الـ dependencies (600 KB+) ومعاه `Logo3D.tsx` و
  `LogoAssemble.tsx` — مكانش فيه أي ملف بيستوردهم.
- **شلت 4 حزم fontsource** لصالح الملفات المباشرة. الـ install أسرع والـ bundle أنضف.

## الأمان

**Next 14.2.15 فيها ثغرة أمنية معلنة** — رفعتها لـ **14.2.35** (نفس الـ minor،
مفيش breaking changes). فاضل تحذيرات في postcss جوه Next نفسه، وحلها بيتطلب Next 16
وده breaking — سيبتها، وهي build-time مش runtime.

**Supabase سيبتها زي ما هي بقصد** (`@supabase/ssr@0.5.2`). النسخة الأحدث 0.12 فيها
API مختلف للكوكيز، ومش هغيّر طبقة الـ auth اللي شغالة عندك دلوقتي من غير ما أقدر
أختبرها على Supabase حقيقي.

---

## الكراش — أهم حاجة في الجولة دي

شغّلت البيلد من **غير أي مفاتيح Supabase** عشان أشوف بيحصل إيه:

```
قبل:  الصفحة الرئيسية 500 · الأسعار 500 · الأسئلة الشائعة 500 · كل صفحة 500
```

**الموقع كله كان بيقع.** السبب إن الـ middleware بيشتغل على كل request، فأي exception
منه = 500 على كل صفحة — بما فيها صفحات التسويق اللي أصلاً مش محتاجة تعرف إنت مين.
وكمان الهيدر بينادي `getSessionUser()` على كل صفحة، وهي كانت بترمي كمان.

ده مش سيناريو نظري: متغيّر ناقص في deploy، مفتاح انتهت صلاحيته، أو Supabase نفسه
واقع لدقيقة = الموقع كله مطفي.

**الحل — سياسة غير متماثلة بقصد:**

- **صفحة عامة** → اعرضها. إننا مش عارفين إنت مين مش مكلّف حاجة هناك.
- **صفحة محمية** → وديه على اللوجين. **fail closed** — عمره ما يدي `/admin` أو
  `/course` لحد لأن الفحص وقع.

وكمان `lib/data.ts` بقى يرجّع قيم فاضية بدل ما يرمي (نصوص الصفحة أصلاً جاية من ملفات
الترجمة مش من الداتابيز، فالصفحة مقروءة تمامًا من غير الجداول).

**النتيجة بعد الإصلاح، من غير أي مفاتيح:**

```
الرئيسية (ar) 200 · الرئيسية (en) 200 · الأسعار 200 · الأسئلة 200
/course       307 → /login    (fail closed، صح)
صفحة مش موجودة 404
```

### صفحات خطأ
- `app/[locale]/error.tsx` — بدل رسالة Next الخام
  ("Application error: a client-side exception has occurred") بالإنجليزي من غير أي
  طريق للرجوع. دلوقتي صفحة بالعربي والإنجليزي فيها زرار "جرّب تاني" و"الرئيسية".
- `app/[locale]/not-found.tsx` — 404 مصمّمة بدل صفحة Next البيضا.

---

## التحقق

```
npm run typecheck   ✓ صفر أخطاء
npm run build       ✓ 35 صفحة، صفر warnings
```

اتأكدت من الـ HTML المبني وهو شغال فعلاً:

| | النتيجة |
|---|---|
| `<html lang="ar" dir="rtl">` / `lang="en" dir="ltr"` | ✓ |
| viewport meta في اللغتين | ✓ |
| preload للخط الصح حسب اللغة | Almarai للعربي · Inter للإنجليزي |
| عناصر sticky في الصفحة | **1** (كانت 2) |
| الـ wordmark ظاهر | ✓ على كل المقاسات |
| `@font-face` في الـ CSS النهائي | 7 إجمالي · صفر Noto Kufi |
| كل الخطوط بتتقدّم من `/fonts/` | ✓ 200 |
| كل الصفحات العامة من غير Supabase | ✓ 200 (كانت 500) |
